## Step 1: Implement hello module with tests
- **Files changed**: `src/__init__.py` (created), `src/hello.py` (created), `tests/test_hello.py` (created)
- **What was built**: Hello module with a single `hello(name: str) -> str` function and unit tests
- **Tests**: 3 unit tests added (test_hello_world, test_hello_empty, test_hello_alice), all passing
- **Notes**: Added `src/__init__.py` to make `src` a proper Python package for imports
